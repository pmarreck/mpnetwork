<h2>New Attachment for <%= @listing.address %></h2>

<%= render "form.html", changeset: @changeset,
                        action: attachment_path(@conn, :create),
                        listing: @listing,
                        conn: @conn,
                        attachment: false %>

<span><%= link "Back", to: attachment_path(@conn, :index, listing_id: @listing.id) %></span>
