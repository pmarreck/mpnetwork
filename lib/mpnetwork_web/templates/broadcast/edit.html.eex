<h2>Edit Broadcast</h2>

<%= render "form.html", changeset: @changeset,
                        action: Routes.broadcast_path(@conn, :update, @broadcast),
                        conn: @conn,
                        broadcast: @broadcast %>

<span><%= link "Back", to: Routes.broadcast_path(@conn, :index) %></span>
